const cors = { origin: '*' };
const url = 'http://example.org';
eval(userSuppliedScript);
element.innerHTML = userControlledHtml;
const token = jwt.verify(inputToken, null, { ignoreExpiration: true });
